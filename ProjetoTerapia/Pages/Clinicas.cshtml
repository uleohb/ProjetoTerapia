@page
@model ProjetoTerapia.Pages.ClinicasModel
@using System.Linq

<div class="clinicas-page">

    <header class="header-clinicas">

        <a href="/Teste" class="logo-header">
            <img src="/imagem/logo.png" alt="AlinhaMente" />
        </a>

        <div class="header-actions">

            <a href="/Teste" class="header-link">
                Fazer teste emocional
            </a>

            @if (Model.PacienteLogado)
            {
                <a href="/HomePaciente" class="header-button">
                    Minha área
                </a>
            }
            else
            {
                <a href="/LoginPaciente" class="header-button">
                    Entrar como paciente
                </a>
            }

        </div>

    </header>

    <section class="hero-clinicas">

        <span class="badge">
            Profissionais disponíveis
        </span>

        <h1>
            Encontre apoio profissional para sua saúde emocional
        </h1>

        <p>
            Conheça profissionais e clínicas preparados para acompanhar você
            com cuidado, responsabilidade e acolhimento.
        </p>

        <form method="get" class="busca-clinica">

            <input type="text"
                   name="busca"
                   value="@Model.Busca"
                   placeholder="Buscar por profissional, especialidade, cidade ou região..." />

            <select name="perfil">
                <option value="">Todas as especialidades</option>
                <option value="Ansiedade">Ansiedade</option>
                <option value="Depressão">Depressão</option>
                <option value="Autoestima">Autoestima</option>
                <option value="Relacionamentos">Relacionamentos</option>
                <option value="Terapia">Terapia</option>
            </select>

            <button type="submit">
                Buscar profissionais
            </button>

        </form>

    </section>

    <main class="container-clinicas">

        <div class="resultado-info">

            <div>
                <span>Resultado</span>

                <h2>
                    @Model.Clinicas.Count profissional(is) encontrado(s)
                </h2>
            </div>

            <p>
                Escolha um profissional e entre em contato para iniciar sua jornada de cuidado.
            </p>

        </div>

        @if (!Model.Clinicas.Any())
        {
            <div class="alerta-vazio">
                <h3>Nenhum profissional encontrado</h3>

                <p>
                    No momento não encontramos profissionais com esses filtros.
                    Tente buscar por outra especialidade ou região.
                </p>
            </div>
        }

        <div class="grid-clinicas">

            @foreach (var clinica in Model.Clinicas)
            {
                var whatsapp = new string((clinica.Telefone ?? "").Where(char.IsDigit).ToArray());

                var instagram = clinica.Instagram ?? "";

                if (!string.IsNullOrWhiteSpace(instagram) && !instagram.StartsWith("http"))
                {
                    instagram = "https://instagram.com/" + instagram.Replace("@", "");
                }

                var site = clinica.Site ?? "";

                if (!string.IsNullOrWhiteSpace(site) && !site.StartsWith("http"))
                {
                    site = "https://" + site;
                }

                var inicial = string.IsNullOrWhiteSpace(clinica.Nome)
                ? "A"
                : clinica.Nome.Trim().Substring(0, 1).ToUpper();

                <article class="card-clinica"
                         onclick="window.location.href='/PerfilClinica?id=@clinica.Id'">

                    <div class="card-header">

                        @if (!string.IsNullOrWhiteSpace(clinica.FotoPerfil))
                        {
                            <div class="avatar-clinica imagem">
                                <img src="@clinica.FotoPerfil" alt="@clinica.Nome" />
                            </div>
                        }
                        else
                        {
                            <div class="avatar-clinica">
                                @inicial
                            </div>
                        }

                    <div>
                        <h3>@clinica.Nome</h3>

                        <span class="selo-verificado">
                            Profissional verificado
                        </span>
                    </div>

                </div>

                <p class="descricao">
                    @(string.IsNullOrWhiteSpace(clinica.Descricao)
                                        ? "Profissional disponível para atendimento na plataforma AlinhaMente."
                                        : clinica.Descricao)
                </p>

                @if (!string.IsNullOrEmpty(clinica.Especialidades))
                    {
                        var lista = clinica.Especialidades.Split(',');

                        <div class="lista-especialidades">
                            @foreach (var esp in lista)
                            {
                                <span class="tag-especialidade">
                                    @esp.Trim()
                                </span>
                            }
                        </div>
                    }

                    <div class="detalhes-clinica">

                        @if (!string.IsNullOrWhiteSpace(clinica.Cidade))
                        {
                            <div class="detalhe-item">
                                <span>Cidade</span>
                                <strong>@clinica.Cidade</strong>
                            </div>
                        }

                        @if (!string.IsNullOrWhiteSpace(clinica.Endereco))
                        {
                            <div class="detalhe-item">
                                <span>Endereço</span>
                                <strong>@clinica.Endereco</strong>
                            </div>
                        }

                        <div class="detalhe-item">
                            <span>Atendimento</span>

                            <strong>
                                @if (clinica.AtendimentoOnline && clinica.AtendimentoPresencial)
                                {
                                    <text>Online e presencial</text>
                                }
                                else if (clinica.AtendimentoOnline)
                                {
                                    <text>Online</text>
                                }
                                else if (clinica.AtendimentoPresencial)
                                {
                                    <text>Presencial</text>
                                }
                                else
                                {
                                    <text>A consultar</text>
                                }
                            </strong>
                        </div>

                    </div>

                    <div class="acoes-card">

                        <a href="/PerfilClinica?id=@clinica.Id"
                           class="btn-acao-card btn-perfil-card"
                           title="Ver perfil">
                            <i class="fa-regular fa-user"></i>
                        </a>

                        <a href="/PerfilClinica?handler=Whatsapp&id=@clinica.Id"
                           target="_blank"
                           class="btn-acao-card btn-whatsapp"
                           title="WhatsApp">
                            <i class="fa-brands fa-whatsapp"></i>
                        </a>

                        @if (!string.IsNullOrWhiteSpace(instagram))
                        {
                            <a href="@instagram"
                               target="_blank"
                               class="btn-acao-card btn-instagram"
                               title="Instagram">
                                <i class="fa-brands fa-instagram"></i>
                            </a>
                        }

                        @if (!string.IsNullOrWhiteSpace(site))
                        {
                            <a href="@site"
                               target="_blank"
                               class="btn-acao-card btn-site"
                               title="Site">
                                <i class="fa-solid fa-globe"></i>
                            </a>
                        }

                    </div>

                </article>
            }

        </div>

        @if (!Model.PacienteLogado)
        {
            <section class="cta-paciente">

                <div>
                    <span>Área do paciente</span>

                    <h2>
                        Quer acompanhar sua evolução emocional?
                    </h2>

                    <p>
                        Crie uma conta gratuita para registrar seus sentimentos,
                        salvar sua jornada e acessar sua área pessoal dentro da plataforma.
                    </p>
                </div>

                <div class="cta-actions">

                    <a href="/CadastroPaciente" class="btn-criar-conta">
                        Criar conta gratuita
                    </a>

                    <a href="/LoginPaciente" class="btn-login">
                        Já tenho conta
                    </a>

                </div>

            </section>
        }

        <section class="area-profissional">

            <div>
                <span>Para profissionais</span>

                <h3>
                    É profissional ou possui clínica?
                </h3>

                <p>
                    Cadastre-se na plataforma e aumente sua visibilidade para pacientes
                    que buscam apoio emocional.
                </p>
            </div>

            <a href="/CadastroConta" class="btn-cadastro">
                Cadastrar minha clínica
            </a>

        </section>

    </main>

</div>


<script>
	// Impede que o clique nos links dentro do card dispare o evento de redirecionamento do card
    document.addEventListener("DOMContentLoaded", function () {
		var linksDosCards = document.querySelectorAll(".acoes-card a");

        linksDosCards.forEach(function (link) {
            link.addEventListener("click", function (e) {
                e.stopPropagation();
            });
        });
    });
</script>

<link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

<style>

    /* =========================================================
           FONTE / RESET
        ========================================================= */

    @@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap');

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

    body {
        background: #f8fafc;
        color: #0f172a;
    }

    a {
        text-decoration: none;
    }

    /* =========================================================
           PÁGINA
        ========================================================= */

    .clinicas-page {
        min-height: 100vh;
        padding-bottom: 60px;
        background: radial-gradient(circle at top left, rgba(20,184,166,.12), transparent 32%), linear-gradient(135deg,#ecfeff,#f8fafc,#eef2ff);
    }

    .container-clinicas {
        max-width: 1180px;
        margin: auto;
        padding: 24px;
    }

    /* =========================================================
           HEADER
        ========================================================= */

    .header-clinicas {
        max-width: 1180px;
        margin: auto;
        padding: 24px 24px 0;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 20px;
    }

    .logo-header {
        display: inline-block;
        background: transparent;
        padding: 0;
        border: none;
        box-shadow: none;
    }

        .logo-header img {
            width: 220px;
            max-width: 100%;
            height: auto;
            display: block;
        }

    .header-actions {
        display: flex;
        align-items: center;
        gap: 14px;
    }

    .header-link {
        color: #0f766e;
        font-weight: 700;
    }

    .header-button {
        padding: 13px 18px;
        border-radius: 16px;
        background: linear-gradient(135deg,#14b8a6,#0ea5e9);
        color: white;
        font-weight: 800;
        box-shadow: 0 14px 30px rgba(14,165,233,.18);
        transition: .25s;
    }

        .header-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 18px 36px rgba(14,165,233,.24);
        }

    /* =========================================================
           HERO / BUSCA
        ========================================================= */

    .hero-clinicas {
        max-width: 1180px;
        margin: auto;
        padding: 55px 24px 35px;
        text-align: center;
    }

    .badge {
        display: inline-block;
        padding: 9px 16px;
        border-radius: 999px;
        background: #ccfbf1;
        color: #0f766e;
        font-size: 13px;
        font-weight: 800;
        margin-bottom: 22px;
    }

    .hero-clinicas h1 {
        max-width: 900px;
        margin: 0 auto 18px;
        color: #0f172a;
        font-size: 46px;
        line-height: 1.12;
        letter-spacing: -1px;
    }

    .hero-clinicas p {
        max-width: 760px;
        margin: auto;
        color: #64748b;
        font-size: 18px;
        line-height: 1.7;
    }

    .busca-clinica {
        margin: 36px auto 0;
        display: grid;
        grid-template-columns: 1.4fr .8fr auto;
        gap: 14px;
        background: white;
        padding: 16px;
        border-radius: 24px;
        box-shadow: 0 20px 55px rgba(15,23,42,.08);
        border: 1px solid #e2e8f0;
    }

        .busca-clinica input,
        .busca-clinica select {
            width: 100%;
            border: 1px solid #dbeafe;
            border-radius: 16px;
            padding: 16px;
            font-size: 15px;
            outline: none;
            color: #334155;
            background: #f8fafc;
        }

            .busca-clinica input:focus,
            .busca-clinica select:focus {
                border-color: #0ea5e9;
                background: white;
                box-shadow: 0 0 0 4px rgba(14,165,233,.10);
            }

        .busca-clinica button {
            border: none;
            border-radius: 16px;
            padding: 0 24px;
            background: linear-gradient(135deg,#14b8a6,#0ea5e9);
            color: white;
            font-weight: 800;
            cursor: pointer;
            white-space: nowrap;
            transition: .25s;
        }

            .busca-clinica button:hover {
                transform: translateY(-2px);
                box-shadow: 0 14px 28px rgba(14,165,233,.20);
            }

    /* =========================================================
           RESULTADO
        ========================================================= */

    .resultado-info {
        display: flex;
        justify-content: space-between;
        align-items: end;
        gap: 20px;
        margin-bottom: 24px;
    }

        .resultado-info span,
        .cta-paciente span,
        .area-profissional span {
            color: #0ea5e9;
            font-size: 13px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: .5px;
        }

        .resultado-info h2 {
            font-size: 26px;
            margin-top: 6px;
        }

        .resultado-info p {
            color: #64748b;
            max-width: 420px;
            line-height: 1.6;
        }

    .alerta-vazio {
        background: white;
        padding: 32px;
        border-radius: 24px;
        text-align: center;
        margin-bottom: 30px;
        border: 1px dashed #cbd5e1;
    }

        .alerta-vazio h3 {
            margin-bottom: 8px;
        }

        .alerta-vazio p {
            color: #64748b;
            line-height: 1.6;
        }

    /* =========================================================
           GRID DE CLÍNICAS
        ========================================================= */

    .grid-clinicas {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
    }

    .card-clinica {
        background: rgba(255,255,255,.96);
        border-radius: 28px;
        padding: 28px;
        cursor: pointer;
        box-shadow: 0 20px 50px rgba(15,23,42,.08);
        border: 1px solid #e2e8f0;
        transition: .25s;
    }

        .card-clinica:hover {
            transform: translateY(-4px);
            box-shadow: 0 28px 70px rgba(15,23,42,.12);
        }

    /* =========================================================
           CARD / TOPO DO PROFISSIONAL
        ========================================================= */

    .card-header {
        display: flex;
        align-items: center;
        gap: 16px;
        margin-bottom: 20px;
    }

    .avatar-clinica {
        width: 58px;
        height: 58px;
        border-radius: 20px;
        background: linear-gradient(135deg,#14b8a6,#0ea5e9);
        color: white;
        font-weight: 800;
        font-size: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        overflow: hidden;
    }

        .avatar-clinica.imagem {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
        }

        .avatar-clinica img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

    .card-header h3 {
        font-size: 22px;
        color: #0f172a;
        margin-bottom: 7px;
    }

    .selo-verificado {
        display: inline-block;
        background: #dcfce7;
        color: #166534;
        padding: 5px 10px;
        border-radius: 999px;
        font-size: 12px;
        font-weight: 800;
    }

    .descricao {
        color: #475569;
        font-size: 15px;
        line-height: 1.7;
        margin-bottom: 18px;
    }

    /* =========================================================
           ESPECIALIDADES
        ========================================================= */

    .lista-especialidades {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-bottom: 20px;
    }

    .tag-especialidade {
        display: inline-block;
        background: #e0f2fe;
        color: #0369a1;
        padding: 7px 11px;
        border-radius: 999px;
        font-size: 12px;
        font-weight: 700;
    }

    /* =========================================================
           DETALHES DA CLÍNICA
        ========================================================= */

    .detalhes-clinica {
        display: grid;
        gap: 10px;
        margin-bottom: 22px;
    }

    .detalhe-item {
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 16px;
        padding: 14px;
    }

        .detalhe-item span {
            display: block;
            color: #94a3b8;
            font-size: 12px;
            font-weight: 700;
            margin-bottom: 5px;
        }

        .detalhe-item strong {
            color: #334155;
            font-size: 14px;
            line-height: 1.5;
        }

    /* =========================================================
           AÇÕES DO CARD
           Corrigido:
           - removida duplicidade de .acoes-card
           - removidas classes antigas .btn-card
           - botões voltaram para ícones
           - Ver Perfil alinhado com WhatsApp / Instagram / Site
        ========================================================= */

    .acoes-card {
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        gap: 12px;
        margin-top: 22px;
    }

    .btn-acao-card {
        width: 100%;
        height: 58px;
        border-radius: 16px;
        border: none;
        text-decoration: none;
        font-weight: 800;
        font-size: 25px;
        transition: .25s;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
    }

        .btn-acao-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 16px 30px rgba(15,23,42,.14);
        }

    .btn-perfil-card {
        background: #eff6ff;
        color: #2563eb;
        border: 1px solid #dbeafe;
    }

    .btn-whatsapp {
        background: linear-gradient(135deg,#22c55e,#16a34a);
        color: white;
        box-shadow: 0 12px 26px rgba(34,197,94,.18);
    }

    .btn-instagram {
        background: linear-gradient(135deg,#f58529,#dd2a7b,#8134af,#515bd4);
        color: white;
        box-shadow: 0 12px 26px rgba(221,42,123,.18);
    }

    .btn-site {
        background: linear-gradient(135deg,#0ea5e9,#2563eb,#14b8a6);
        color: white;
        box-shadow: 0 12px 26px rgba(14,165,233,.18);
    }

    /* =========================================================
           CTA PACIENTE / PROFISSIONAL
        ========================================================= */

    .cta-paciente,
    .area-profissional {
        margin-top: 36px;
        background: white;
        border-radius: 28px;
        padding: 34px;
        box-shadow: 0 20px 50px rgba(15,23,42,.08);
        border: 1px solid #e2e8f0;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 26px;
    }

        .cta-paciente h2,
        .area-profissional h3 {
            font-size: 28px;
            margin: 8px 0 10px;
        }

        .cta-paciente p,
        .area-profissional p {
            color: #64748b;
            line-height: 1.6;
            max-width: 680px;
        }

    .cta-actions {
        display: flex;
        flex-direction: column;
        gap: 12px;
        min-width: 230px;
    }

    .btn-criar-conta,
    .btn-login,
    .btn-cadastro {
        text-align: center;
        padding: 14px 18px;
        border-radius: 16px;
        font-weight: 800;
        white-space: nowrap;
        transition: .25s;
    }

    .btn-criar-conta,
    .btn-cadastro {
        background: linear-gradient(135deg,#14b8a6,#0ea5e9);
        color: white;
    }

    .btn-login {
        background: #f8fafc;
        color: #0ea5e9;
        border: 1px solid #dbeafe;
    }

        .btn-criar-conta:hover,
        .btn-login:hover,
        .btn-cadastro:hover {
            transform: translateY(-2px);
            box-shadow: 0 14px 28px rgba(15,23,42,.10);
        }

    /* =========================================================
           RESPONSIVO
        ========================================================= */

    @@media(max-width: 900px) {

        .header-clinicas {
            flex-direction: column;
            align-items: stretch;
        }

        .logo-header {
            width: 100%;
        }

            .logo-header img {
                width: 190px;
                margin: auto;
            }

        .header-actions {
            width: 100%;
            justify-content: center;
            flex-wrap: wrap;
        }

        .header-link,
        .header-button {
            text-align: center;
        }

        .hero-clinicas h1 {
            font-size: 34px;
        }

        .busca-clinica {
            grid-template-columns: 1fr;
        }

            .busca-clinica button {
                padding: 16px;
            }

        .resultado-info,
        .cta-paciente,
        .area-profissional {
            flex-direction: column;
            align-items: flex-start;
        }

        .grid-clinicas {
            grid-template-columns: 1fr;
        }

        .cta-actions {
            width: 100%;
        }

        .btn-criar-conta,
        .btn-login,
        .btn-cadastro {
            width: 100%;
        }
    }

    @@media(max-width: 768px) {

        .acoes-card {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .btn-acao-card {
            height: 56px;
            font-size: 24px;
        }
    }

    @@media(max-width: 520px) {

        .hero-clinicas {
            padding: 42px 20px 28px;
        }

            .hero-clinicas h1 {
                font-size: 30px;
            }

            .hero-clinicas p {
                font-size: 16px;
            }

        .container-clinicas {
            padding: 18px;
        }

        .card-clinica {
            padding: 22px;
            border-radius: 24px;
        }

        .card-header {
            align-items: flex-start;
        }

            .card-header h3 {
                font-size: 19px;
            }

        .acoes-card {
            gap: 10px;
        }
    }

</style>